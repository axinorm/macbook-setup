- name: Set name
  git_config:
    name: user.name
    scope: global
    value: "{{ git_username }}"

- name: Set email address
  git_config:
    name: user.email
    scope: global
    value: "{{ git_email }}"